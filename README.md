# Animal-species-image-recognition-
Animal Species Image Recognition Web App built with TensorFlow and FastAPI. Uses MobileNetV2 transfer learning to classify animal images via a REST API and simple web interface. Includes training pipeline, inference module, unit tests, and GitHub Actions CI for automated testing and deployment readiness.
